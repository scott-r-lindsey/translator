# Translator

A small Linux desktop shell for live subtitles and translation.

## Setup

```bash
uv sync --dev
```

## Run

```bash
uv run translator-shell
```

## Check

```bash
uv run pytest
uv run ruff check .
uv run pyright
```
